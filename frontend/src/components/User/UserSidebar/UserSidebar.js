import React from 'react';
import Sidebar from '../../UI/Sidebar/Sidebar';
import USERSIDEBARDATA from './UserSidebarData';

const UserSidebar = () => {
  return <Sidebar data={USERSIDEBARDATA} />;
};

export default UserSidebar;
